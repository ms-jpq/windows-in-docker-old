<network>
  <name>${NAT_NAME}</name>
  <bridge name="${NAT_NAME}"/>
  <forward mode="nat">
    <nat>
      <port start="1" end="65000"/>
    </nat>
  </forward>
  <ip address="${ROUTER}" netmask="${MASK}">
    <dhcp>
      <range start="${BEGIN}" end="${END}"/>
    </dhcp>
  </ip>
</network>

