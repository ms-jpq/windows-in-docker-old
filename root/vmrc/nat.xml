<network>
  <name>${NAT_NAME}</name>
  <bridge name="${NAT_NAME}"/>
  <forward mode="nat"/>
  <ip address="${ROUTER}" netmask="${MASK}">
    <dhcp>
      <range start="${BEGIN}" end="${END}"/>
    </dhcp>
  </ip>
</network>

