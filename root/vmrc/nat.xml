<network>
  <name>${VIRT_NAT_NAME}</name>
  <bridge name="${VIRT_NAT_NAME}"/>
  <forward mode="nat"/>
  <ip address="${ROUTER}" netmask="${MASK}">
    <dhcp>
      <range start="${BEGIN}" end="${END}"/>
    </dhcp>
  </ip>
</network>

